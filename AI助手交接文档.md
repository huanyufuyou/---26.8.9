# AI 助手交接文档（给 Cursor 接手）

> **读完本文再干活。** 目的：新对话第一条消息就能接住「我是谁、学什么、学到哪、现在该干啥」。  
> 更新日期：2026-09-01  
> 本文件所在仓库：`github.com/huanyufuyou/---26.8.9`（电赛复盘 + F570 计划文档库）  
> 用户本机还有更大工程目录（见下文路径），**不全在本 GitHub 仓库里**。

---

## 一、我是谁

- 嵌入式方向，**大二刚开学**（2026-09）。
- 电赛 26 年 E 题《拼图装置》**省三**；队伍心态偏「今年练手、明年展示」。
- 自我技术叙述权威来源（必读口径）：
  - CSDN：《电赛初体验，回眸来时路》  
    https://blog.csdn.net/2501_94715694/article/details/163613427  
  - 本仓库：`电赛学习总结.txt`（同文整理）
- **PID / 串级 / 前馈 / 惯导 / 状态机 / 时间片 / 五层架构** 是熟练主武器；**LQR 只跑通最小链，核心（Riccati/最优性）仍浅**——勿当零基础，也勿当已精通 LQR。
- 讲解偏好：自然语言 + 物理直觉 + 演进箭头，再点名公式；对齐其文「换了个皮」「会调参 vs 懂控制」。
- 工程习惯：STM32 偏好 **SPL**（非明确要求不用 HAL/LL）；MISRA-C 意识；改控制代码先讲清再写；自我探索任务不代写核心。

### Cursor 规则（用户机）

- `C:\Users\18971\.cursor\rules\user-d1-tech-background.mdc`（大一背景 + 讲解口径，alwaysApply）
- `C:\Users\18971\.cursor\rules\control-theory-note.mdc`（讲控制时交叉 `d:\Control_Theory_Note_cn.pdf`）
- 另有：`D:\F570\.cursor\rules\` 下可能有同类控制讲义规则

---

## 二、当前总目标（双主线 + 共享线）

| 线 | 内容 | 本机主目录 |
|----|------|------------|
| A 四轴 | WHEELTEC F570 教学机：读固件、LQR/PID、实飞、后续自研四旋翼 | `D:\F570\` |
| B 工创赛 | 27 智能网联汽车设计（NEVC-SimOne），国特代码改规控 | `D:\工创赛-27\` |
| 共享 | LQR（MATLAB/Python 仿真 → 车横向 / 机姿态） | 两边共用 |
| 文档/复盘 | 电赛总结、计划、交接 | 本仓库 + `D:\diansai_yuxing\` |

**就业叙事（用户焦虑点，已对齐）：**  
社团多用现成飞控调参；公司多在 PX4 等开源栈二次开发。F570 练的是「懂控制」底子，不是浪费；PX4/成熟飞控是后补「工程皮」。近期**不要**把 RL/MPC/PX4 塞进主线抢带宽。

---

## 三、主线 A：F570 四轴（进度快照）

### 已完成（重要）

- Keil / ARMCC 环境、工程拷贝到 `D:\F570\` 可编可烧。
- **PID 版**飞过；有基线 hex：`D:\F570\baseline\baseline_PID_能飞_20260829.hex`。
- **工厂 LQR 版**能悬；光流定点**有残余漂移是原厂预期**（手册写明非 100% 静止）。
- **自己的 K 最小链已闭环（约 2026-08-29）**：MATLAB `LQR_OF.m` → 写入 `balance_task.c` → 实飞。
  - 曾把水平 `Q11=100`（约 2× 原厂）→ 猛纠偏后 **滚转翻跟头摔落**。
  - 已安全回退：水平 `Q11=25`、高度 `Q11=5`；固件贴回近原厂增益；漂移变小，用户表示「最小开发链路满意」。
- 惯量进 LQR：物理上是 `角加速度 = 力矩/I` → `B=[0;1/I]` → `dlqr`；水平通道用 `g`，高度用 `1/m`，姿态才用 `I`。
- 飞控定位：WHEELTEC **教学专用固件**（FreeRTOS + 互补/Mahony 味估姿 + 分通道 PID/LQR），**不是 PX4 换皮**。

### 硬件约束

- **无板载电池**，长线地面供电；飞行受线缆范围限制（非机械系留，但也非自由飞）。
- PS2：摇杆需大幅偏转；方向键是零点微调；空杆过常因手柄没电。

### 关键路径

```
D:\F570\LQR_SPL\WHEELTEC_LQR\          LQR 固件
  WHEELTEC_APP\balance_task.c          增益 kp/kd,k11..k42；姿态/位置控制
D:\F570\MATLAB_LQR\MATLAB程序\LQR_OF.m  光流版分通道 dlqr（改完可直接读此文件）
D:\F570\笔记_LQR物理参数与转动惯量.md
D:\F570\F570学习笔记.docx
原厂手册（百度网盘资料包）含定高/定点/光流说明
讲义：d:\Control_Theory_Note_cn.pdf
```

### 增益映射（背一下）

| MATLAB | 固件 |
|--------|------|
| Kx≈ | kp, kd（水平） |
| K1 | k11, k12（高度） |
| K2 | k21, k22（Roll） |
| K3 | k31, k32（Pitch） |
| K4 | k41, k42（Yaw） |

近原厂量级参考：`kp/kd≈0.49/0.45`（Q=25）、`k11/k12≈0.49/0.79`、`k21..≈0.083/0.014`。

### F570 近期建议状态

- **降优先级**（工创赛窗口期）：保持「能烧能飞」即可，勿同时深挖 Riccati + 改机 + PX4。
- 若再调：一次只动一类通道、幅度小；先烧稳再比现象。

---

## 四、主线 B：27 工创赛网联车（当前应抬音量）

### 赛项理解（已用手册/PPT 校正）

- 平台：NEVC-SimOne；算法输出 **油门/刹车/转向** 等，经 API `SetDrive`。
- **车质量/摩擦多在仿真器车模内**；国特学生侧转向是 **纯跟随（几何）**，`VehicleParam` 只有轴距类，不显式写质量。
- 架构应拆开：感知/地图事件 → **决策规划（路径意图）** → **控制执行（贴线/刹停）**。  
  LQR 主要帮 **横向跟踪**，不要求算法里先建模摩擦。
- 评分：基础关键任务未完成可 **直接 0 分**；先保能完成再谈更优。
- 赛程：校赛报名审核后才批量开平台账号；用户判断 **还有约半个月～一个月可读代码**，平台未开也正常。

### 已完成

- 本机平台/环境、国特代码整合、多模块可编译（详见旧交接 `D:\diansai_yuxing\AI助手交接文档.md`）。
- LKA 等领读过；纯跟随在 `UtilDriver.hpp`（前瞻 `max(v·0.8, 3)` 一类逻辑，轴距硬编码 `1.3+1.55`）。
- Python 可视化预演：`D:\工创赛-27\lqr_sim\`（自行车模型 + LQR，`--test` 曾 PASS）。目标：以后替换纯跟随转向。

### 明确不要做（近期）

- **不要**把强化学习当工创赛主线。
- **不要**现在上 MPC / 深啃 PX4。
- LQR 数学深挖让位于：**读懂国特主路径 + 能改规则相关点**。

### 这半个月建议动作

1. 读国特：AEB / LKA / APA / 高阶题主循环（感知→决策→SetDrive）。
2. 自画一张数据流图。
3. 搞清评分与「关键任务」。
4. 有余力：`lqr_sim` 对齐轴距、纯跟随 vs LQR 对比（路线图已写在 `lqr_sim/README.md`）。

### 关键路径

```
D:\工创赛-27\网联汽车(1)\网联汽车\     国特整合工程
D:\工创赛-27\官方ADAS备份\            官方骨架备份
D:\工创赛-27\lqr_sim\                 LQR 横向仿真预演
D:\工创赛-27\网联车赛项分享文件\       ppt_text.txt / pdf_text.txt
D:\diansai_yuxing\AI助手交接文档.md   上一版交接（细节更全，部分进度需以本文为准）
```

---

## 五、LQR 学习：真实水位（勿夸大）

**已有：** 状态空间直觉、Q/R 调参实飞教训、惯量/质量进 B、F570 最小链、车横向 `lqr_sim`。  
**没有：** 能对外讲清 Riccati、最优性、完整输入输出公式链、前馈与 LQR 的边界（部分仍黑盒）。  
**错误简化：** 「LQR = 更快出参的 PID」——应说成「带模型的状态反馈设计路径」。  
讲义路径长：先状态空间/离散/代价，再 LQR；空着正常。

---

## 六、协作与节奏约定

1. **一步一确认**；数学/原理先于改增益或改固件。
2. 用户改完 `.m` / 增益：助手应 **直接读文件**，少让用户贴数。
3. 赛期临近或用户明确主线时：**只跟一条主音量**（当前默认倾向：工创赛读码 > F570 深挖）。
4. 编码：改完尽量编译到 0 Error（Keil 工程在用户 Windows 本机）。
5. 用户若另开对话：让其 @ 或打开本文件 + 对应工程文件夹。

---

## 七、本仓库与 IDE 打开说明（2026-09-01）

- 本 GitHub 仓库文件少：`电赛学习总结.txt`、`F570学习计划.txt`、`电赛学习.code-workspace`、本文。
- 用户曾遇 Cursor 报错：`AppData\Roaming\Cursor\Workspaces\1787835624841\...` 路径不存在 → **最近工作区缓存坏了**。  
  恢复：不要点坏最近项；**打开文件夹**到本机克隆，或打开 `电赛学习.code-workspace`。
- `D:\F570`、`D:\工创赛-27` 若要一起开，需另做多根工作区（本机路径），不必强行塞进本小仓库。

---

## 八、给接手助手的「第一条回复」建议

1. 确认用户此刻主线：工创赛读码 / F570 / 只问理论 / 其它。  
2. 若工创赛：从国特某模块主循环领读或列读码顺序，不开 RL。  
3. 若 F570：先确认当前烧的是安全回退增益，再谈下一步。  
4. 语气对齐其 CSDN：白话、物理、演进，少一上来砸矩阵。

---

## 九、一句话现状

> 大二开学；F570「MATLAB→K→实飞」最小链已过关并收工感强；LQR 核心仍浅；工创赛平台未开但应读国特；近期主音量偏网联车交付准备，RL/PX4/MPC 冷冻；PID 仍是比赛主武器。
